# 用于增大 gradle 可用内存。
org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Fabric 属性
# 在此查看最新版本：https://fabricmc.net/develop
minecraft_version=26.1
loader_version=0.17.3
loom_version=1.15-SNAPSHOT

# Fabric API / Carpet（实际值由 versions/*/gradle.properties 按版本提供）
carpet_core_version=[VERSIONED]

# 模组属性
mod_version=0.1.0
maven_group=com.example.carpet
archives_base_name=SafeSave
